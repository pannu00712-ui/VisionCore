<UserControl x:Class="VisionCore.WPF.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:VisionCore.WPF.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:LogsViewModel, IsDesignTimeCreatable=False}"
             Background="{StaticResource Brush.Background}"
             FontFamily="{StaticResource Font.Body}"
             FontSize="{StaticResource FontSize.MD}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Level → badge background colour -->
        <Style x:Key="LevelBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding"      Value="5,1"/>
            <Setter Property="Width"        Value="22"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <!-- default: info -->
            <Setter Property="Background" Value="{StaticResource Brush.AccentLight}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding LevelKey}" Value="Warning">
                    <Setter Property="Background" Value="{StaticResource Brush.WarningLight}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Error">
                    <Setter Property="Background" Value="{StaticResource Brush.DangerLight}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Fatal">
                    <Setter Property="Background" Value="{StaticResource Brush.Danger}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Debug">
                    <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Verbose">
                    <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Level text colour -->
        <Style x:Key="LevelText" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="{StaticResource FontSize.XS}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource Brush.Accent}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding LevelKey}" Value="Warning">
                    <Setter Property="Foreground" Value="{StaticResource Brush.Warning}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Error">
                    <Setter Property="Foreground" Value="{StaticResource Brush.Danger}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Fatal">
                    <Setter Property="Foreground" Value="White"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Debug">
                    <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding LevelKey}" Value="Verbose">
                    <Setter Property="Foreground" Value="{StaticResource Brush.Text.Disabled}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- toolbar -->
            <RowDefinition Height="*"/>     <!-- log list -->
            <RowDefinition Height="Auto"/>  <!-- status bar -->
        </Grid.RowDefinitions>

        <!-- ── Toolbar ───────────────────────────────────────────────────────── -->
        <Border Grid.Row="0"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="0,0,0,1"
                Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>  <!-- page title -->
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="160"/>   <!-- level filter -->
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="160"/>   <!-- source filter -->
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="*"/>     <!-- search box -->
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>  <!-- action buttons -->
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Logs"
                           Style="{StaticResource Text.Heading}"
                           VerticalAlignment="Center"/>

                <!-- Level filter -->
                <ComboBox Grid.Column="2"
                          ItemsSource="{Binding LevelFilterOptions}"
                          SelectedItem="{Binding LevelFilter}"
                          Height="32"
                          ToolTip="Filter by log level"/>

                <!-- Source filter -->
                <TextBox Grid.Column="4"
                         Text="{Binding SourceFilter, UpdateSourceTrigger=PropertyChanged}"
                         Height="32"
                         VerticalContentAlignment="Center"
                         ToolTip="Filter by source (class name)">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <!-- placeholder text via watermark trick -->
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Search -->
                <Grid Grid.Column="6">
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Height="32"
                             VerticalContentAlignment="Center"
                             Padding="10,0,32,0"
                             ToolTip="Search message text"/>
                    <!-- Reset filter icon -->
                    <Button Style="{StaticResource Btn.Icon}"
                            Command="{Binding ResetFilterCommand}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Width="26" Height="26"
                            Margin="0,0,3,0"
                            ToolTip="Clear all filters">
                        <TextBlock Text="✕" FontSize="10"
                                   Foreground="{StaticResource Brush.Text.Secondary}"/>
                    </Button>
                </Grid>

                <!-- Action buttons -->
                <StackPanel Grid.Column="8"
                            Orientation="Horizontal"
                            VerticalAlignment="Center">

                    <!-- Pause/Resume -->
                    <Button Style="{StaticResource Btn.Secondary}"
                            Command="{Binding PauseCommand}"
                            MinWidth="80"
                            Margin="0,0,6,0">
                        <Button.Content>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Pause"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPaused}" Value="True">
                                                <Setter Property="Text" Value="Resume"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button.Content>
                    </Button>

                    <!-- Copy selected -->
                    <Button Content="Copy"
                            Style="{StaticResource Btn.Ghost}"
                            Command="{Binding CopyLineCommand}"
                            Margin="0,0,6,0"
                            ToolTip="Copy selected entry to clipboard"/>

                    <!-- Export -->
                    <Button Content="Export"
                            Style="{StaticResource Btn.Ghost}"
                            Command="{Binding ExportCommand}"
                            Margin="0,0,6,0"
                            ToolTip="Export visible log entries to .log file"/>

                    <!-- Clear -->
                    <Button Content="Clear"
                            Style="{StaticResource Btn.Ghost}"
                            Command="{Binding ClearCommand}"
                            ToolTip="Clear local log buffer"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ── Log list ───────────────────────────────────────────────────────── -->
        <ListView Grid.Row="1"
                  x:Name="LogListView"
                  ItemsSource="{Binding Entries}"
                  SelectedItem="{Binding Selected}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  BorderThickness="0"
                  Background="{StaticResource Brush.Background}"
                  FontFamily="{StaticResource Font.Mono}"
                  FontSize="{StaticResource FontSize.SM}"
                  SelectionMode="Single">

            <ListView.View>
                <GridView>
                    <!-- Time -->
                    <GridViewColumn Header="Time" Width="88">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FormattedTimestamp}"
                                           FontFamily="{StaticResource Font.Mono}"
                                           FontSize="{StaticResource FontSize.SM}"
                                           Foreground="{StaticResource Brush.Text.Secondary}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Level badge -->
                    <GridViewColumn Header="Lvl" Width="36">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource LevelBadge}">
                                    <TextBlock Text="{Binding LevelBadge}"
                                               Style="{StaticResource LevelText}"/>
                                </Border>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Source -->
                    <GridViewColumn Header="Source" Width="180">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Source}"
                                           FontFamily="{StaticResource Font.Mono}"
                                           FontSize="{StaticResource FontSize.SM}"
                                           Foreground="{StaticResource Brush.Text.Secondary}"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding Source}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Message -->
                    <GridViewColumn Header="Message" Width="900">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Message}"
                                           FontFamily="{StaticResource Font.Mono}"
                                           FontSize="{StaticResource FontSize.SM}">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding LevelKey}" Value="Warning">
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.Warning}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelKey}" Value="Error">
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.Danger}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelKey}" Value="Fatal">
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.Danger}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelKey}" Value="Debug">
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.Text.Secondary}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding LevelKey}" Value="Verbose">
                                                    <Setter Property="Foreground" Value="{StaticResource Brush.Text.Disabled}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>

            <!-- Empty state -->
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Entries.IsEmpty}" Value="True">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <Border Background="{StaticResource Brush.Background}">
                                            <StackPanel HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"
                                                        Margin="0,80">
                                                <TextBlock Text="📋"
                                                           FontSize="40"
                                                           HorizontalAlignment="Center"
                                                           Margin="0,0,0,12"/>
                                                <TextBlock Text="No log entries match the current filters."
                                                           Style="{StaticResource Text.Secondary}"
                                                           HorizontalAlignment="Center"/>
                                            </StackPanel>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>

            <!-- Row style: alternate banding + hover -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding"                    Value="4,2"/>
                    <Setter Property="BorderThickness"            Value="0"/>
                    <Setter Property="Background"                 Value="Transparent"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource Brush.SurfaceAlt}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{StaticResource Brush.AccentLight}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- ── Status bar ─────────────────────────────────────────────────────── -->
        <Border Grid.Row="2"
                Background="{StaticResource Brush.Surface}"
                BorderBrush="{StaticResource Brush.Border}"
                BorderThickness="0,1,0,0"
                Padding="16,6">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Paused indicator -->
                    <Border Style="{StaticResource Badge.Warning}"
                            Margin="0,0,8,0"
                            Visibility="{Binding IsPaused, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="PAUSED"
                                   Foreground="{StaticResource Brush.Warning}"
                                   FontSize="{StaticResource FontSize.XS}"
                                   FontWeight="Bold"/>
                    </Border>

                    <TextBlock Text="{Binding StatusMessage}"
                               Style="{StaticResource Text.Secondary}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Auto-scroll toggle -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <CheckBox Content="Auto-scroll"
                              IsChecked="{Binding AutoScroll}"
                              VerticalAlignment="Center"
                              Margin="0,0,16,0"/>
                    <TextBlock Foreground="{StaticResource Brush.Text.Disabled}"
                               FontSize="{StaticResource FontSize.SM}"
                               VerticalAlignment="Center">
                        <Run Text="{Binding FilteredCount, Mode=OneWay}"/>
                        <Run Text=" / "/>
                        <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                        <Run Text=" entries"/>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
